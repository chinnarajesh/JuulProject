<aura:component implements="flexipage:availableForAllPageTypes,lightning:isUrlAddressable" access="global">
    <aura:attribute type="String" name="propertyValue"/>
    <aura:attribute type="String" name="recordId"/>
    <aura:attribute type="String" name="accountId"/>
    <aura:attribute type="boolean" name="doReload" default="false"/>
    <aura:handler name="init" value="{!this}" action="{!c.init}"/>   
  
    <aura:if isTrue="{!v.doReload}">
        <div class="slds-card">
            <c:accountMissionControlFlowLWC propertyValue="{!v.propertyValue}" AccAuraID="{!v.accountId}"
               onclose="{!c.closeMethodInAuraController}" onrefresh="{!c.refreshMethodInAuraController}"/>
        </div>
    </aura:if>
</aura:component>